[general]
static=yes
writeprotect=no
clearglobalvars=no

[globals]

[default]
exten => 300,	1,Noop(Test Extension!)
same =>			n,Playback(demo-instruct)
same =>			n,Hangup()

exten => 301,	1,Noop(Test Extension!)
same =>			n,Playback(hello)
same =>			n,Hangup()
